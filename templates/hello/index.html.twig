{% extends 'base.html.twig' %}

{% block title %}Hello HelloController!{% endblock %}

{% block body %}
<div class="row">
    <div class="col-lg-6 offset-lg-3">
        <h1>Hello {{ found_person.name|default('Personne') }} !</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-secondary">Chercher</button>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
